using UnityEngine;

public class PayableSaver : MonoBehaviour
{
    // // Start is called once before the first execution of Update after the MonoBehaviour is created
    // private Payable payable;

    // void Start()
    // {
    //     payable = GetComponent<Payable>();

    //     LoadPayable();

    //     payable.SetDiamondText();
    // }

    // // Update is called once per frame
    // void Update()
    // {

    // }

    // public void SavePayable()
    // {
    //     PlayerPrefs.SetFloat("Diamond", payable.diamond);
    //     PlayerPrefs.SetInt("CoinFever", payable.coinFever ? 1 : 0);
    //     PlayerPrefs.SetInt("NoAdvertisement", payable.noAdvertisement ? 1 : 0);
    // }

    // public void LoadPayable()
    // {
    //     payable.diamond = PlayerPrefs.GetFloat("Diamond", 0f);
    //     payable.coinFever = PlayerPrefs.GetInt("CoinFever", 0) == 1;
    //     payable.noAdvertisement = PlayerPrefs.GetInt("NoAdvertisement", 0) == 1;
    // }

}
